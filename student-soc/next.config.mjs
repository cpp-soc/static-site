/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.licdn.com', 'encrypted-tbn0.gstatic.com', 'cppsoc.xyz', 'github.com'],
      }
};

export default nextConfig;
